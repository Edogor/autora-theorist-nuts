# src/autora/theorist/nuts/genetic/crossover.py
import copy, random

def subtree_crossover(a, b, max_depth):
    a = copy.deepcopy(a)
    b = copy.deepcopy(b)
    # TODO: pick random nodes; swap if depth constraint holds
    # return a_swapped
    return a
